labels:
  osid: ubuntu
actions:
  - pkg: 
      names: bzip2 build-essential dkms
      update: true
    after: apt install -y linux-headers-$(uname -r)
  - shell: "{{ .templates }}/network_ubuntu.sh"

  